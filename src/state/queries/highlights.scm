(header_keyword) @keyword

(version) @number

(quoted_path) @string.special.path

(escape_sequence) @string.escape

[
  (year)
  (month)
  (day)
  (hour)
  (minute)
  (second)
] @number

(invalid_record) @error
