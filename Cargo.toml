[package]
name = "tree-sitter-logrotate"
description = "Logrotate configuration grammar for Tree-sitter"
version = "0.1.0"
authors = ["Brandon Williams <willibrandon@gmail.com>"]
license = "MIT"
readme = "README.md"
keywords = ["incremental", "parsing", "tree-sitter", "logrotate"]
categories = ["parser-implementations", "parsing", "text-editors"]
repository = "https://github.com/willibrandon/tree-sitter-logrotate"
edition = "2024"
autoexamples = false

build = "bindings/rust/build.rs"
include = [
  "bindings/rust/*",
  "grammar.js",
  "queries/*",
  "src/*",
  "tree-sitter.json",
  "/LICENSE",
]

[lib]
path = "bindings/rust/lib.rs"

[dependencies]
tree-sitter-language = "0.1"

[build-dependencies]
cc = "1.2"

[dev-dependencies]
tree-sitter = "0.26.12"
